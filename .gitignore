# Dependencias
node_modules/

# Variables de entorno (secretos)
.env
.env.local

# Base de datos local
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Binarios de descarga reales (no versionar artefactos pesados)
downloads/*.AppImage
downloads/*.deb
downloads/*.rpm
downloads/*.tar.gz

# Logs y sistema
*.log
npm-debug.log*
.DS_Store
Thumbs.db
